function allInOne(numOne, numTwo, numThree){
    let sumup = sum(numOne, numTwo, numThree)
    let allsum = subtract(numOne, numTwo, numThree)


function sum(numOne, numTwo, numThree){
    return numOne + numTwo
}

function subtract(numOne, numTwo, numThree){
    return sumup - numThree
}
console.log(allsum);
}